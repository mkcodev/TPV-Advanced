# apps/mobile — Expo (app de camareros)

Aún sin generar. Genera con el scaffolder oficial:

```bash
cd apps/mobile
pnpm create expo@latest .
```

Comparte tipos y lógica con `@tpv/core`, `@tpv/api` y `@tpv/validators`.
Función principal: tomar comandas desde el móvil y sincronizarlas en tiempo real.
