# apps/desktop — Electron (la caja / TPV descargable)

Aún sin generar. Es una CÁSCARA fina que envuelve la UI de React (la misma de `apps/web`)
y añade: impresión ESC/POS, cajón portamonedas, base de datos local (SQLite) y offline.

Se montará en la Fase 2 (cuando la UI del TPV esté madura en web).
Sugerencia: `electron` + `electron-builder` (auto-update) + `node-thermal-printer`.
